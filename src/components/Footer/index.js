import React, { Component } from 'react';
import './style.css';

const Footer = () => (
  <footer className="mainFooter">
    <div className="container">
      Projekt zrealizowano w ramach zajęć z przedmiotu "Programowanie aplikacji webowych SPA"
    </div>
  </footer>
);

export default Footer;